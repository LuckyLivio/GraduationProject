# 项目工作记录

## 2026-09-17 · 持续失配门控与机制边界复核

- 在 `fedac77` 提交并推送实现与预先固定协议后，使用冻结的 142/242/342 模型运行新面板：5 条件 × 4 方法 × 18 布局 × 3 模型，共 1,080 个方法回合，257.0 秒。历史实验保持原样，累计 2,250 个方法回合。
- 新增持续门控、迟滞退出，以及使用同样有效转移过滤和历史窗口的物理辨识基线。独立名义验证集确定阈值，测试后未调阈值、未选择模型。
- 整体变化下门控 H16 位置 RMSE 0.0498，冻结模型 0.7715；原环境门控 0.0406，始终校准 0.0480，冻结模型 0.0351。名义精度仅部分恢复；局部变化始终校准更好。
- 导航结果除冻结模型在局部变化条件下为 18/18、17/18、17/18 外全部 18/18，未证明门控控制表现更优。噪声导航 54/54 回合曾触发，42.64% 动作前处于校准状态，明确保留负结果。
- 定向核验 PETS、GrBAL/ReBAL、CaDM、RMA、GP 残差 MPC；15 条访问记录中 14 条成功，Page 原文未能访问，只核验书目。当前设计不声称首创。
- Web 增加门控面板、运行途中修改阻尼且保留历史、模型种子标识与报告入口。实际 Edge 测试第 8 步施加变化，第 13 步启用校准，无状态重置；桌面/手机无异常与溢出。
- 52 项 Python 测试：51 通过、1 Windows 符号链接夹具权限跳过。分析脚本核对原始回合数、配对预测窗口、预算、终止原因、误差与检测汇总；全部一致。
- 运行后发现离线回放的 score 与 gate/scale 字段时序不同，保留原始回放，派生对齐版本及哈希说明；不修改任何模型、阈值、动作或实验指标。后续 runner 修复该显示字段。
- 结果与局限见 `gated-findings.md`，图表 PNG/PDF 与原始记录随 Git 发布。下一阶段为持续性/退出消融和噪声下可辨识性实验，仍保留独立新测试面板。

## 2026-09-17 · 从选题进入可运行验证

用户确认：智能科学与技术本科，约 10 周后答辩；展示偏好为“机器人预见未来”，技术要求为智能方法及可论证的创新。今天先完成小系统并启动关键对照，尽早判断选题是否可行。

### 本日协议

- 真实 2D 运动环境，3 个移动障碍；固定未知空间阻尼场，域外测试仅将强度乘以 1.7。
- 第一版采用**混合动力学世界模型**：3 个小型 MLP 从交互转移推断阻尼，已知的动作积分和障碍反弹规律保留。这是为了快速验证而收缩的实现，不能称为端到端学会整个环境。
- 先学习位置相关阻尼，模型推理不调用环境的真实阻尼函数；简单全局物理模型和近期历史局部辨识模型作为强对照。
- 自适应机制只调整预测视野；固定短/中/长视野、探测成本与候选预测预算全部记录。
- 训练、验证、预测测试、规划器验证、导航测试使用独立种子来源。先用规划器验证回合确定固定视野参照，再评估测试集。
- 今日首轮只有一个独立训练运行，3 个集成成员不等于 3 个独立实验种子；结果仅用于可行性诊断。后续改参数不能再把这批诊断种子当最终论文盲测。
- 显示真实预测与执行记录。交互界面当前为可回放实验，实时推理入口可在此基础上增加。

### 与早期计划的差异

第一版未使用 5 步历史 MLP，也未端到端预测全部状态；冻结的学习模型只输入当前位置，物理辨识基线读取过去转移。这一不对称给予物理基线更强的在线适应能力，需要明确报告。仅在先导实验确有需求时增加历史模型；不把该修改提前当作完成。

控制周期为 0.15 秒，最大 100 步，视野集合为 5/10/16 步。模型在 CPU 上训练和执行以减少小批量 GPU 同步开销；已检测 GPU 可用，但不凭设备名称推断实时性能。

### 验证与留痕

- 环境与规划器 14 项单元测试通过，覆盖随机种子、碰撞穿越、反弹、动作作用、历史辨识、视野响应及预算约束。
- 实验入口：`python -m foresight.experiment`。先写 `protocol.json`，再生成训练记录、验证结果、每局原始指标与实际回放。
- Git 按可检查的阶段提交；数据集和权重保留本地，配置与紧凑评测结果上传。每次提交哈希和结果文件可用于追溯。

### 当天已完成结果

- 首轮 CPU 训练与评测完成：12,993 转移，144 测试回合，全流程 50.40 秒。学习模型分布内多步预测有效，自适应视野未胜出。
- 三个独立训练运行复核：142/242/342，各自训练 3 成员集成，在新面板共完成 864 回合。原分布成功率饱和；阻尼变化后自适应仍未优于固定方法。
- 因负结果提出并预先记录新的单机制探索：固定模型权重，最近 5 条有效转移校准空间阻尼倍率，固定 H=10，另用新面板完成 162 回合。效果及失败边界见 `latest-findings.md`。
- Web 小系统支持真实在线推理、目标修改、方法/场景/阻尼切换与独立实验回放。首轮约 17KB 模型随仓库发布，训练数据与其他检查点保留本地。
- 浏览器实测回放、实时推理、目标修改；桌面和 390px 手机检查无横向溢出、无 JavaScript 异常。页面截图保存在 `artifacts/demo`。
- 代码、实验、分析和界面分别提交并推送，不覆盖已保存批次；实验脚本增加已存在目录保护。
- 最终验证：33 项 Python 测试中 32 项通过，1 项因 Windows 不允许创建符号链接测试夹具而跳过；浏览器验证残差校准真实推理与点击改目标通过。GitHub 保存全部阶段提交。

下一步聚焦“何时启用校准”和非整体缩放环境下的边界，不把已有在线辨识思想包装成首次提出。
